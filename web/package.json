{
  "name": "amazonian-design-surface-web",
  "version": "0.1.0",
  "private": true,
  "description": "Internationalised GitHub Pages showcase for the amazonian-design-surface collection.",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "build:catalog": "node ../scripts/build-catalog.mjs",
    "validate:naming": "node ../scripts/validate-naming.mjs",
    "validate:locales": "node ../scripts/validate-locales.mjs"
  },
  "devDependencies": {
    "astro": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
