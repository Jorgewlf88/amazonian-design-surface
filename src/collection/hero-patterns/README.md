# Hero patterns
Complex focal graphics that carry the story of a collection. Large repeat (24-64 cm), high motif density, clear focal hierarchy.

One folder per design, named as its base identifier without the version. See docs/nomenclature.md. `meta.example.json` in this folder is a reference record, not a real asset.
